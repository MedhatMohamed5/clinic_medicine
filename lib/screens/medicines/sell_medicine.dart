import 'package:flutter/material.dart';

class SellMedicineScreen extends StatelessWidget {
  const SellMedicineScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Container(
          child: Text('Sell Medicine'),
        ),
      ),
    );
  }
}
